dbType=mysql # mysql或pgsql  注：mariadb也是填mysql
backtime=2 # # 每隔多少分钟进行一次备份  如果是一个float类型 则固定对应时间进行操作  如果是HH:mm类型 则每天该时间进行操作(UTC时间！）
port=3306 #端口
host=127.0.0.1 #服务器地址
dbs=ss,hhx #数据库
user=root #用户
pwd=pwd #密码
savedir=/ #保存路径 相对于程序当前的执行路径 比如 /test/或者/test2/t/
maxfiles=50 # 最大保存的文件数量 默认为50